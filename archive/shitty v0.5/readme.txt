this was for a school project where we weren't allowed to use a server
to serve module scripts with proper CORs headers, so it all had to be classic
scripts and it would only run on Firefox (and maybe Safari).